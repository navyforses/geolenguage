# ===========================================
# ციფრული ოლიგოპოლია - კონფიგურაცია
# ===========================================

# სერვერის პარამეტრები
PORT=3001
NODE_ENV=development

# ===========================================
# უფასო API Keys (რეგისტრაცია საჭირო)
# ===========================================

# Finnhub - News & Sentiment
# რეგისტრაცია: https://finnhub.io/register
# უფასო: 60 requests/minute
FINNHUB_API_KEY=your_finnhub_key_here

# Alpha Vantage - Stock Data (backup)
# რეგისტრაცია: https://www.alphavantage.co/support/#api-key
# უფასო: 25 requests/day
ALPHA_VANTAGE_KEY=your_alpha_vantage_key_here

# ===========================================
# არ საჭიროებს API Key-ს (უფასო)
# ===========================================
# Yahoo Finance - ავტომატურად მუშაობს
# Reddit API - ავტომატურად მუშაობს
# SEC EDGAR - ავტომატურად მუშაობს
# Wikipedia - ავტომატურად მუშაობს

# ===========================================
# არასავალდებულო (დამატებითი ფუნქციები)
# ===========================================

# Redis (კეშირებისთვის - არასავალდებულო)
# თუ არ არის, მეხსიერებაში კეშირება იმუშავებს
REDIS_URL=redis://localhost:6379

# PostgreSQL (მონაცემთა ბაზა - არასავალდებულო)
# თუ არ არის, in-memory storage იმუშავებს
DATABASE_URL=postgresql://user:password@localhost:5432/oligopoly

# JWT (ავთენტიფიკაციისთვის)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d

# ===========================================
# სწრაფი დაწყება (მინიმალური კონფიგურაცია)
# ===========================================
# მხოლოდ ეს 2 ცვლადი საჭიროა სრული ფუნქციონალისთვის:
#
# 1. გადააკოპირე ეს ფაილი .env სახელით:
#    cp .env.example .env
#
# 2. დაარეგისტრირდი Finnhub-ზე და ჩასვი API key:
#    FINNHUB_API_KEY=your_key
#
# 3. გაუშვი სერვერი:
#    npm run dev
#
# Yahoo Finance და Reddit მუშაობს API key-ის გარეშე!
